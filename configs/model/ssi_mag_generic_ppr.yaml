name: ssi_mag_generic_ppr
version: ssi_mag_generic_ppr

hidden_dim: 256
dropout: 0.2
norm: layernorm

max_order: 3
num_layers: \${model.max_order}
diffusion_add_self_loops: true
global_prior_restart: 0.15
global_prior_order: 2
fusion: concat_residual_mlp
eps: 1.0e-8
control_mode: ppr_style
ablation: full
